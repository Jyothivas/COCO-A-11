//const Atm = require('../../lib/jasmine_examples/Atm');

 
describe('Atm machine', function () {



    it('Check given atm  withdrawbalance',function(){
        const accountwithdrawbalance = new accountwithdrawbalance();
        expect(debit.isdebit('account')).toEqual('Enter a check account');
        expect(balance.isbalance(500)).toEqual('withdraw');
        expect(balance.isbalance(2000)).toEqual('withdraw');
        expect(balance.isbalance(1000)).toEqual('withdraw');
        expect(balance.isbalance(0)).toEqual('withdraw');        

    });
});
